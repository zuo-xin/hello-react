import React from 'react'
import styles from  './carousel.less'
//const classNames = require('classnames')


class Carousel extends React.Component{
	render(){

		return(
			<p className={`${styles.aaa} ${styles.bbb}`}>轮播图</p>
		)
	}
}

export default Carousel
